export default async function getFTString(home, homeScore, scorers, away, awayScore) {
  return `🚩 FT: ${home} ${homeScore}-${awayScore} ${away} 

⚽ Goalscorers: ${scorers}

👉 Follow Player of the Match`
}

